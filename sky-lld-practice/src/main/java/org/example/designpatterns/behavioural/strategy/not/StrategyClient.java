package org.example.designpatterns.behavioural.strategy.not;

public class StrategyClient {

    // Problem
    //If the child classes is using parent cap is fine. If they are overriding the cap and
    //duplicating the same code in multiple child classes is not correct in growing organization

    // Solution
    // Create Strategy interface and declare the common implementation and use it in different
    //classes
    public static void main(String[] args) {

    }
}
